{
  "name": "verify",
  "version": "0.1.0",
  "purpose": "Run the project's tests and lint; return one normalized JSON diagnostic stream.",
  "reach_for_me_when": [
    "you finished editing code and need to know if it is correct",
    "you are about to claim tests pass, code works, or a fix is complete",
    "you need structured failure locations instead of parsing raw test output"
  ],
  "commands": [
    {"name": "run", "summary": "Detect adapters, run them, emit diagnostics",
     "args": "[--root <dir>] [--changed-only] [--junit <xml>]"},
    {"name": "self-test", "summary": "Prove the tool works", "args": ""}
  ],
  "danger_level": "read_only",
  "cache": "off",
  "deps": {"python": [], "system": ["git"], "optional": ["ruff"]}
}
