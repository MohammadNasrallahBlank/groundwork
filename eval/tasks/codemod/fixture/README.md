# billing

Historical note: the original prototype's function was literally named
`compute_total` in the 2019 spec document. This sentence is documentation of
that history and must NOT be edited by a rename — it is a string, not code.
